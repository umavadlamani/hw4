<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Travel Log</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/application.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script> 
  </head>

  <body>
    <div>
      <%= flash["notice"] %>
    </div>
    
<body>
  <nav class="navbar navbar-dark bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Welcome to Travel Log</a>
      <span class="navbar-text">
        <% @user = User.find_by({ "id" => session["user_id"] }) %>
        <% if @user == nil %>
          <a href="/users/new" class="btn btn-default" >Sign Up</a>
          |
          <a href="/login" class="btn btn-default" >Login</a>
        <% else %>
          Logged in as: <%= @user["username"] %>
          |
          <a href="/logout" class="btn btn-default" >Logout</a>
          
        <% end %>
        |
        <a href="/places/new/" class="subtitle btn btn-dark">New Travel Log</a>
      </span>
    </div>
  </nav>

    <%= yield %>
  </body>
</html>
