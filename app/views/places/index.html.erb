<h1>Places</h1>

<% if @current_user == nil%>
  Please log in to see a list of places
<% else %>
  <ul>
    <% for place in @places %>
      <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
    <% end %>
  </ul>

  <p>
    <a href="/places/new">New place</a>
  </p>
<% end %>